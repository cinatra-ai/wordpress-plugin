# Code owners for cinatra-ai/wordpress-plugin.
# Routine paths have NO code owner: no reviewer is auto-requested on ordinary PRs.
# Security-relevant paths request the owner plus the org review account, so a
# change to CI/workflows/gates or the dependency manifest gets explicit review.
# Reference: https://docs.github.com/articles/about-code-owners

# Security-relevant paths — request the owner + the org review account.
/.github/ @groganz @megajustice
/LICENSE @groganz @megajustice

# Installer / site-write risk surface (cinatra-ai/cinatra#2021 S6). cinatra.php
# is this plugin's single source file — the forthcoming one-click plugin
# installer (side-loads and installs code onto the customer's own WordPress
# site) lands here, so the whole file is covered ahead of that PR to make
# review structurally required, not just requested by convention.
/cinatra.php @groganz @megajustice
